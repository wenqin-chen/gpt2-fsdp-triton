# Read by slurm/full_run.sbatch when the job STARTS (not at submission), so the calibration results can
# still choose the parallel mode and micro-batch of the queued full run.
PARALLEL=fsdp
MICRO_BATCH=64
EXTRA=""
